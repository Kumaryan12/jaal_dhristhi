# Comma-separated browser origins allowed to call the API.
JAALDRISHTI_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Local defaults. Docker Compose overrides these with mounted container paths.
JAALDRISHTI_DB_PATH=data/jaaldrishti.db
JAALDRISHTI_MODEL_PATH=models/ecosystem-risk-model.joblib
