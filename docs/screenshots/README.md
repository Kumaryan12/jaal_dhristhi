# Release Screenshot Contract

Phase 10 requires real product captures. Do not substitute mockups or generated images.

Capture these states through the in-app browser at a desktop viewport after the API is populated with seed 2026:

1. `executive-dashboard.png` — `/` with all four metrics, risk mix, and activity trend visible.
2. `emerging-risk-demo.png` — `/demo` after running the simulation, with LOW/HIGH cards and the network visible.

Before committing, verify that no browser chrome, private credentials, local file paths, or non-synthetic personal data appear. Reference the captures from the root README and Phase 10 report only after both files exist.
